describe( 'operations model', function() {
  it( 'loads one operation from db' );
  it( 'loads a query of operation from db' );
  it( 'write an operation from db' );
  it( 'edit an operation from db' );
  it( 'deletes an operation from db' );
});
