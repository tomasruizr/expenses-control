describe( 'List Component', function() {
  it( 'renders an array of objects' );
  it( 'Adds an element to the lists' );
  it( 'edit an element of the lists' );
  it( 'deletes an element of the lists' );
});
